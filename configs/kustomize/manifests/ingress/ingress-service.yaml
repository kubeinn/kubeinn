# Strip prefix /postgrest 
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: ingress-stripprefix
spec:
  stripPrefix:
    prefixes:
      - /postgrest
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-service
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - http:
        paths:
          - path: /innkeeper
            backend:
              serviceName: innkeeper-cluster-ip-service
              servicePort: 80
          - path: /pilgrim
            backend:
              serviceName: pilgrim-cluster-ip-service
              servicePort: 80
          - path: /grafana
            backend:
              serviceName: grafana-cluster-ip-service
              servicePort: 3000
          - path: /api
            backend:
              serviceName: schutterij-cluster-ip-service
              servicePort: 8080
          - path: /postgrest
            backend:
              serviceName: postgrest-cluster-ip-service
              servicePort: 8080
          - path: /
            backend:
              serviceName: crossroads-cluster-ip-service
              servicePort: 80
